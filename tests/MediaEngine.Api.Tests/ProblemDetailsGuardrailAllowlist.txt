# ProblemDetails wire-shape guardrail allowlist.
# Format: one repo-relative Endpoints/*.cs file path per line.
# Blank lines and lines starting with # are ignored.
#

# EMPTY by design: stage 5A wave 2 converted every endpoint file onto
# MediaEngine.Api.Http.ApiErrors. New entries are forbidden — handled errors
# must return RFC7807 problem bodies via ApiErrors.
