@namespace Tanaste.Web.Components.Settings

@* Property Mapper — retired. Field mapping is now part of the Connection Vault.
   This component is kept for backward compatibility with deep-link URLs.
   Settings.razor redirects PropertyMapper → ConnectionVaultTab. *@

<MudAlert Severity="Severity.Info" Variant="Variant.Outlined" Class="mb-4">
    Field mapping is now managed in the <strong>Connection Vault</strong>.
    Expand any provider to configure its field mappings, test connections, and fetch properties.
</MudAlert>

@code {
    // Intentionally empty — this tab is deprecated.
    // Settings.razor renders ConnectionVaultTab for the PropertyMapper route.
}
