@namespace MediaEngine.Web.Shared

@* NavMenu is no longer rendered — the sidebar has been replaced by the
   floating Intent Dock and the Command Palette (Ctrl+K).
   This file is kept as a minimal stub so the project compiles cleanly. *@
