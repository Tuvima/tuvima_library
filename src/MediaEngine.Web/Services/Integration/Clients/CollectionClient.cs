namespace MediaEngine.Web.Services.Integration.Clients;

// Staged extraction marker for collection endpoints that still depend on legacy collection mapping helpers.
public sealed class CollectionClient;
