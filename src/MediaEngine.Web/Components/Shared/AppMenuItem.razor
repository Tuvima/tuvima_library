@namespace MediaEngine.Web.Components.Shared

<MudMenuItem Icon="@Icon" Disabled="@Disabled" OnClick="HandleClick" Class="@RootClass">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        @Label
    }
</MudMenuItem>

@code {
    [Parameter] public string? Label { get; set; }
    [Parameter] public string? Icon { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public EventCallback OnClick { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public AppUiTone Tone { get; set; } = AppUiTone.Neutral;

    private string RootClass => string.Join(" ", new[] { "app-menu-item", AppUiMaps.ToneClass(Tone) });

    private Task HandleClick(MouseEventArgs _) => OnClick.InvokeAsync();
}
