@namespace MediaEngine.Web.Components.Shared

<div class="@RootClass">
    @if (!string.IsNullOrWhiteSpace(Label))
    {
        <label class="app-field__label" for="@InputId">@Label</label>
    }

    <MudNumericField T="int"
                     InputId="@InputId"
                     Value="@Value"
                     ValueChanged="OnValueChanged"
                     Variant="Variant.Outlined"
                     Margin="Margin.Dense"
                     Disabled="@Disabled"
                     ReadOnly="@ReadOnly"
                     FullWidth="@FullWidth"
                     Min="@(Min ?? int.MinValue)"
                     Max="@(Max ?? int.MaxValue)"
                     Step="@Step"
                     Class="app-control app-numeric-field" />

    @if (!string.IsNullOrWhiteSpace(HelpText))
    {
        <div class="app-field__help">@HelpText</div>
    }
</div>

@code {
    private readonly string InputId = $"app-number-{Guid.NewGuid():N}";

    [Parameter] public string? Label { get; set; }
    [Parameter] public int Value { get; set; }
    [Parameter] public EventCallback<int> ValueChanged { get; set; }
    [Parameter] public string? HelpText { get; set; }
    [Parameter] public int? Min { get; set; }
    [Parameter] public int? Max { get; set; }
    [Parameter] public int Step { get; set; } = 1;
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public bool ReadOnly { get; set; }
    [Parameter] public bool FullWidth { get; set; } = true;
    [Parameter] public AppControlSize Size { get; set; } = AppControlSize.Normal;
    [Parameter] public string? Class { get; set; }

    private string RootClass =>
        string.Join(" ", new[] { "app-field", AppUiMaps.SizeClass(Size), Class }
            .Where(value => !string.IsNullOrWhiteSpace(value)));

    private Task OnValueChanged(int value) => ValueChanged.InvokeAsync(value);
}
