@namespace MediaEngine.Web.Components.Shared
@using System.Linq
@using MediaEngine.Web.Services.Playback

<section class="@RootClass" aria-label="@AriaLabel" @attributes="AdditionalAttributes">
    @ChildContent
</section>

@code {
    [Parameter]
    public PlaybackExperience Experience { get; set; } = PlaybackExperience.Music;

    [Parameter]
    public string AriaLabel { get; set; } = "Playback mini player";

    [Parameter]
    public string? Class { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    [Parameter(CaptureUnmatchedValues = true)]
    public IReadOnlyDictionary<string, object>? AdditionalAttributes { get; set; }

    private string ExperienceClass => Experience switch
    {
        PlaybackExperience.Audiobook => "playback-mini-player--audiobook",
        PlaybackExperience.Video => "playback-mini-player--video",
        _ => "playback-mini-player--music",
    };

    private string RootClass => string.Join(" ", new[]
    {
        "playback-mini-player",
        ExperienceClass,
        Class,
    }.Where(value => !string.IsNullOrWhiteSpace(value)));
}
