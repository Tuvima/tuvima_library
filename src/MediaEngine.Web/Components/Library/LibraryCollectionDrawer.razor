@using MediaEngine.Web.Models.ViewDTOs
@namespace MediaEngine.Web.Components.Library
@inject IStringLocalizer<SharedStrings> L

@* ── Backdrop ── *@
<div class="collection-drawer-backdrop" @onclick="OnClose"></div>

@* ── Drawer Panel ── *@
<div class="collection-drawer" @onclick:stopPropagation="true">

    @* ── 1. Pinned Header ── *@
    <div class="collection-drawer-header">
        <MudIconButton Icon="@Icons.Material.Outlined.Close"
                       Size="Size.Small"
                       Style="position:absolute;top:32px;right:32px;color:rgba(255,255,255,0.5);"
                       OnClick="OnClose" />

        <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;">
            <span class="collection-type-chip" style="border-color:@Collection.TypeColor;color:@Collection.TypeColor;background:@(Collection.TypeColor)1A;">
                @Collection.TypeLabel
            </span>
            <span class="collection-status-pill" style="border-color:@Collection.StatusColor;color:@Collection.StatusColor;background:@(Collection.StatusColor)1A;">
                @Collection.StatusLabel
            </span>
        </div>

        <div style="font-size:24px;font-weight:900;color:white;letter-spacing:-0.02em;margin-top:8px;padding-right:48px;line-height:1.2;">
            @Collection.Name
        </div>

        <div style="font-size:10px;color:rgba(255,255,255,0.35);text-transform:uppercase;letter-spacing:0.08em;margin-top:4px;">
            @(Collection.Scope == "user" ? L["VaultCollection_ScopePerUser"] : L["VaultCollection_ScopeLibrary"])
        </div>
    </div>

    @* ── 2. Scrollable Body ── *@
    <div class="collection-drawer-body">

        @* ── Section 1: CONFIGURATION ── *@
        <div class="collection-section">
            <button class="collection-section-header" @onclick="ToggleConfigSection">
                <div style="display:flex;align-items:center;gap:12px;">
                    <MudIcon Icon="@Icons.Material.Outlined.Settings" Style="font-size:16px;color:rgba(255,255,255,0.5);" />
                    <span class="collection-section-title">@L["VaultCollection_SectionConfiguration"]</span>
                </div>
                <MudIcon Icon="@(_configOpen ? Icons.Material.Outlined.ExpandMore : Icons.Material.Outlined.ChevronRight)"
                         Style="font-size:16px;color:rgba(255,255,255,0.35);" />
            </button>

            @if (_configOpen)
            {
                <div class="collection-section-body">
                    @if (Collection.CollectionType == "Smart")
                    {
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelRules"]</span>
                            <div style="display:flex;flex-wrap:wrap;gap:4px;justify-content:flex-end;">
                                @foreach (var rule in GetRuleChips())
                                {
                                    <span class="collection-rule-chip">@rule</span>
                                }
                            </div>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelGenerationThreshold"]</span>
                            <span class="collection-config-value">@Collection.MinItems</span>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelFeatured"]</span>
                            <MudSwitch T="bool"
                                       Value="@Collection.IsFeatured"
                                       ValueChanged="@(v => OnFeaturedToggled.InvokeAsync((Collection.Id, v)))"
                                       Color="Color.Warning"
                                       UnCheckedColor="Color.Default" />
                        </div>
                        <div class="collection-config-row" style="border-bottom:none;">
                            <span class="collection-config-label">@L["VaultCollection_LabelShowInBrowsing"]</span>
                            <MudSwitch T="bool"
                                       Value="@Collection.IsEnabled"
                                       ValueChanged="@(v => OnEnabledToggled.InvokeAsync((Collection.Id, v)))"
                                       Color="Color.Success"
                                       UnCheckedColor="Color.Default" />
                        </div>
                    }
                    else if (Collection.CollectionType == "System")
                    {
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelMediaTypes"]</span>
                            <div style="display:flex;flex-wrap:wrap;gap:4px;justify-content:flex-end;">
                                @foreach (var chip in GetSystemMediaChips())
                                {
                                    <span class="collection-rule-chip">@chip</span>
                                }
                            </div>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelProgressTracking"]</span>
                            <span class="collection-config-value" style="color:#5DCAA5;">@L["VaultCollection_ValueEnabled"]</span>
                        </div>
                        <div class="collection-config-row" style="border-bottom:none;">
                            <span class="collection-config-label">@L["VaultCollection_LabelItems"]</span>
                            <span class="collection-config-value">@Collection.ItemCount</span>
                        </div>
                    }
                    else if (Collection.CollectionType == "Mix")
                    {
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelLogic"]</span>
                            <span class="collection-config-value" style="font-style:italic;color:rgba(255,255,255,0.5);">@(Collection.Description ?? "—")</span>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelRefreshSchedule"]</span>
                            <span class="collection-config-value" style="color:rgba(255,255,255,0.5);">@(Collection.RefreshSchedule ?? L["VaultCollection_ValueDaily"])</span>
                        </div>
                        <div class="collection-config-row" style="border-bottom:none;">
                            <span class="collection-config-label">@L["VaultCollection_LabelGenerateForAllUsers"]</span>
                            <MudSwitch T="bool"
                                       Value="@Collection.IsEnabled"
                                       ValueChanged="@(v => OnEnabledToggled.InvokeAsync((Collection.Id, v)))"
                                       Color="Color.Success"
                                       UnCheckedColor="Color.Default" />
                        </div>
                    }
                    else if (Collection.CollectionType == "Playlist")
                    {
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelOwner"]</span>
                            <span class="collection-config-value">@(Collection.Scope == "user" ? L["VaultCollection_OwnerUser"] : L["VaultCollection_OwnerLibrary"])</span>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelMediaTypes"]</span>
                            <span class="collection-config-value" style="font-style:italic;color:rgba(255,255,255,0.5);">@L["VaultCollection_ValueDerivedFromContents"]</span>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelItems"]</span>
                            <span class="collection-config-value">@Collection.ItemCount</span>
                        </div>
                        <div class="collection-config-row">
                            <span class="collection-config-label">@L["VaultCollection_LabelCreated"]</span>
                            <span class="collection-config-value">@Collection.CreatedAt.ToString("yyyy-MM-dd")</span>
                        </div>
                        <div class="collection-config-row" style="border-bottom:none;">
                            <span class="collection-config-label">@L["VaultCollection_LabelLastModified"]</span>
                            <span class="collection-config-value">@(Collection.ModifiedAt?.ToString("yyyy-MM-dd") ?? "—")</span>
                        </div>
                    }
                </div>
            }
        </div>

        @* ── Section 2: ITEMS PREVIEW ── *@
        <div class="collection-section">
            <button class="collection-section-header" @onclick="ToggleItemsSection">
                <div style="display:flex;align-items:center;gap:12px;">
                    <MudIcon Icon="@Icons.Material.Outlined.List" Style="font-size:16px;color:rgba(255,255,255,0.5);" />
                    <span class="collection-section-title">@L["VaultCollection_SectionItemsPreview"]</span>
                </div>
                <MudIcon Icon="@(_itemsOpen ? Icons.Material.Outlined.ExpandMore : Icons.Material.Outlined.ChevronRight)"
                         Style="font-size:16px;color:rgba(255,255,255,0.35);" />
            </button>

            @if (_itemsOpen)
            {
                <div class="collection-section-body">
                    @if (Items.Count == 0)
                    {
                        <div style="font-size:11px;color:rgba(255,255,255,0.3);font-style:italic;padding:8px 0;">
                            @L["VaultCollection_NoItemsYet"]
                        </div>
                    }
                    else
                    {
                        @foreach (var item in Items)
                        {
                            <div class="collection-item-row">
                                <MudIcon Icon="@GetMediaTypeIcon(item.MediaType)"
                                         Style="font-size:14px;color:rgba(255,255,255,0.4);flex-shrink:0;" />
                                <div style="flex:1;min-width:0;">
                                    <div style="font-size:11px;font-weight:700;color:white;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">
                                        @item.Title
                                    </div>
                                    @if (!string.IsNullOrEmpty(item.Creator))
                                    {
                                        <div style="font-size:9px;color:rgba(255,255,255,0.4);font-style:italic;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">
                                            @item.Creator
                                        </div>
                                    }
                                </div>
                            </div>
                        }
                    }

                    @if (Collection.ItemCount > 0)
                    {
                        <div style="margin-top:12px;">
                            <span style="font-size:10px;font-weight:900;color:#C9922E;cursor:pointer;letter-spacing:0.04em;">
                                @L["VaultCollection_ViewAllItems", Collection.ItemCount] →
                            </span>
                        </div>
                    }
                </div>
            }
        </div>

        @* ── Section 3: ASSETS ── *@
        <div class="collection-section">
            <button class="collection-section-header" @onclick="ToggleAssetsSection">
                <div style="display:flex;align-items:center;gap:12px;">
                    <MudIcon Icon="@Icons.Material.Outlined.Image" Style="font-size:16px;color:rgba(255,255,255,0.5);" />
                    <span class="collection-section-title">@L["VaultCollection_SectionAssets"]</span>
                </div>
                <MudIcon Icon="@(_assetsOpen ? Icons.Material.Outlined.ExpandMore : Icons.Material.Outlined.ChevronRight)"
                         Style="font-size:16px;color:rgba(255,255,255,0.35);" />
            </button>

            @if (_assetsOpen)
            {
                <div class="collection-section-body">
                    @for (var i = 0; i < _assetTypes.Length; i++)
                    {
                        var assetType = _assetTypes[i];
                        var isLast    = i == _assetTypes.Length - 1;
                        <div class="collection-config-row" style="@(isLast ? "border-bottom:none;" : "")">
                            <span class="collection-config-label">@assetType</span>
                            <span style="font-size:11px;color:rgba(255,255,255,0.3);font-style:italic;">
                                @(assetType == "Cover Art" ? L["VaultCollection_AssetAutoComposed"] : L["VaultCollection_AssetDropOrUpload"])
                            </span>
                        </div>
                    }
                </div>
            }
        </div>

    </div>

    @* ── 3. Pinned Action Bar ── *@
    <div class="collection-drawer-actions">
        @if (Collection.CollectionType == "Smart")
        {
            <div style="display:flex;gap:8px;">
                <button class="collection-action-btn collection-action-btn--outlined" style="flex:1;"
                        @onclick="ToggleEnabled">
                    @(Collection.IsEnabled ? L["VaultCollection_ActionDisable"] : L["VaultCollection_ActionEnable"])
                </button>
                <button class="collection-action-btn collection-action-btn--filled" style="flex:1;"
                        @onclick="ToggleFeatured">
                    @(Collection.IsFeatured ? L["VaultCollection_ActionUnfeature"] : L["VaultCollection_ActionFeature"])
                </button>
            </div>
        }
        else if (Collection.CollectionType == "System")
        {
            <div style="text-align:center;">
                <span style="font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:0.1em;color:rgba(255,255,255,0.25);">
                    @L["VaultCollection_SystemManaged"]
                </span>
            </div>
        }
        else if (Collection.CollectionType == "Mix")
        {
            <button class="collection-action-btn collection-action-btn--outlined" style="width:100%;"
                    @onclick="ToggleEnabled">
                @(Collection.IsEnabled ? L["VaultCollection_ActionDisable"] : L["VaultCollection_ActionEnable"])
            </button>
        }
        else if (Collection.CollectionType == "Playlist")
        {
            <div style="display:flex;flex-direction:column;align-items:center;gap:6px;">
                <span style="font-size:10px;color:rgba(255,255,255,0.3);">@L["VaultCollection_ManagedByOwner"]</span>
                <span style="font-size:11px;font-weight:900;color:#C9922E;cursor:pointer;letter-spacing:0.04em;">
                    @L["VaultCollection_OpenInMyLibrary"] →
                </span>
            </div>
        }
    </div>

</div>

@code {
    [Parameter] public ManagedCollectionViewModel Collection { get; set; } = null!;
    [Parameter] public List<CollectionItemViewModel> Items { get; set; } = [];
    [Parameter] public EventCallback OnClose { get; set; }
    [Parameter] public EventCallback<(Guid Id, bool Enabled)> OnEnabledToggled { get; set; }
    [Parameter] public EventCallback<(Guid Id, bool Featured)> OnFeaturedToggled { get; set; }

    // Section open/closed state — individual booleans avoids Razor string-escape issues
    private bool _configOpen = true;
    private bool _itemsOpen  = true;
    private bool _assetsOpen = false;

    private static readonly string[] _assetTypes = ["Cover Art", "Headshot", "Banner", "Logo", "Background"];

    private void ToggleConfigSection() => _configOpen = !_configOpen;
    private void ToggleItemsSection()  => _itemsOpen  = !_itemsOpen;
    private void ToggleAssetsSection() => _assetsOpen = !_assetsOpen;

    private Task ToggleEnabled()  => OnEnabledToggled.InvokeAsync((Collection.Id, !Collection.IsEnabled));
    private Task ToggleFeatured() => OnFeaturedToggled.InvokeAsync((Collection.Id, !Collection.IsFeatured));

    private List<string> GetRuleChips()
    {
        if (!string.IsNullOrWhiteSpace(Collection.RuleJson))
        {
            try
            {
                var rules = System.Text.Json.JsonSerializer.Deserialize<List<string>>(Collection.RuleJson);
                if (rules is { Count: > 0 }) return rules;
            }
            catch { /* malformed JSON — fall through */ }
        }

        return [Collection.Description ?? Collection.CollectionType];
    }

    private List<string> GetSystemMediaChips()
    {
        if (!string.IsNullOrWhiteSpace(Collection.Description))
        {
            var parts = Collection.Description.Split(
                ',',
                System.StringSplitOptions.RemoveEmptyEntries | System.StringSplitOptions.TrimEntries);
            if (parts.Length > 0)
                return [.. parts];
        }

        return ["Books", "Comics"];
    }

    private static string GetMediaTypeIcon(string mediaType) => mediaType.ToUpperInvariant() switch
    {
        "MOVIE"     => Icons.Material.Outlined.Movie,
        "EPUB"      => Icons.Material.Outlined.MenuBook,
        "BOOK"      => Icons.Material.Outlined.MenuBook,
        "AUDIOBOOK" => Icons.Material.Outlined.Headphones,
        "TV"        => Icons.Material.Outlined.LiveTv,
        "MUSIC"     => Icons.Material.Outlined.MusicNote,
        "COMIC"     => Icons.Material.Outlined.AutoStories,
        _           => Icons.Material.Outlined.InsertDriveFile,
    };
}

<style>
    .collection-drawer-backdrop {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.8);
        backdrop-filter: blur(8px);
        z-index: 10000;
    }

    .collection-drawer {
        position: fixed;
        top: 0;
        right: 0;
        width: 36rem;
        height: 100vh;
        background: #0d0f12;
        border-left: 1px solid rgba(255, 255, 255, 0.04);
        z-index: 10001;
        display: flex;
        flex-direction: column;
        animation: slideInFromRight 0.4s ease;
    }

    @@keyframes slideInFromRight {
        from { transform: translateX(100%); }
        to   { transform: translateX(0); }
    }

    .collection-drawer-header {
        position: relative;
        padding: 40px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.03);
        background: rgba(15, 23, 42, 0.1);
        flex-shrink: 0;
    }

    .collection-drawer-body {
        flex: 1;
        overflow-y: auto;
        scrollbar-width: thin;
        scrollbar-color: rgba(255, 255, 255, 0.08) transparent;
    }

    .collection-drawer-actions {
        padding: 32px;
        border-top: 1px solid rgba(255, 255, 255, 0.03);
        background: #0d0f12;
        flex-shrink: 0;
    }

    .collection-type-chip {
        font-size: 9px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        padding: 3px 8px;
        border-radius: 4px;
        border: 1px solid;
    }

    .collection-status-pill {
        font-size: 9px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        padding: 3px 8px;
        border-radius: 20px;
        border: 1px solid;
    }

    .collection-section {
        border-bottom: 1px solid rgba(255, 255, 255, 0.03);
    }

    .collection-section-header {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 20px;
        background: transparent;
        border: none;
        cursor: pointer;
        color: white;
        transition: background 0.15s ease;
    }

    .collection-section-header:hover {
        background: rgba(255, 255, 255, 0.02);
    }

    .collection-section-title {
        font-size: 9px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: rgba(255, 255, 255, 0.5);
    }

    .collection-section-body {
        padding: 0 24px 24px 24px;
    }

    .collection-config-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 8px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.03);
        gap: 16px;
    }

    .collection-config-label {
        font-size: 9px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: rgba(255, 255, 255, 0.4);
        flex-shrink: 0;
    }

    .collection-config-value {
        font-size: 11px;
        font-weight: 700;
        color: white;
        text-align: right;
    }

    .collection-rule-chip {
        font-size: 9px;
        font-weight: 700;
        padding: 2px 7px;
        border-radius: 4px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.08);
        color: rgba(255, 255, 255, 0.7);
        white-space: nowrap;
    }

    .collection-item-row {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 8px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.03);
    }

    .collection-item-row:last-of-type {
        border-bottom: none;
    }

    .collection-action-btn {
        font-size: 11px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        padding: 10px 20px;
        border-radius: 6px;
        cursor: pointer;
        transition: opacity 0.15s ease;
    }

    .collection-action-btn:hover {
        opacity: 0.85;
    }

    .collection-action-btn--outlined {
        background: transparent;
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: rgba(255, 255, 255, 0.7);
    }

    .collection-action-btn--filled {
        background: #C9922E;
        border: 1px solid #C9922E;
        color: #0d0f12;
    }
</style>

