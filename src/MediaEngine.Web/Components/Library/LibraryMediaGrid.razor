@using MediaEngine.Web.Models.ViewDTOs

@if (Items.Count == 0 && !Loading)
{
    <div class="vmg-empty">
        <MudIcon Icon="@Icons.Material.Outlined.Inbox" Style="font-size: 3rem; opacity: 0.15; margin-bottom: 12px;" />
        <p style="color: var(--tl-text-faint); font-size: 0.85rem;">No items match your filters.</p>
    </div>
}
else
{
    <div class="vmg-grid">
        @foreach (var item in Items)
        {
            <LibraryMediaCard Item="@item"
                            @key="item.EntityId"
                            OnCardClicked="OnCardClicked"
                            OnDeleteClicked="OnDeleteClicked" />
        }
    </div>

    @if (Loading)
    {
        <div class="vmg-loading">
            <MudProgressCircular Indeterminate="true" Size="Size.Small"
                                 Style="color: var(--tl-border-strong);" />
        </div>
    }
}

@code {
    [Parameter] public List<LibraryItemViewModel> Items { get; set; } = [];
    [Parameter] public bool Loading { get; set; }
    [Parameter] public EventCallback<Guid> OnCardClicked { get; set; }
    [Parameter] public EventCallback<Guid> OnDeleteClicked { get; set; }
}