@page "/listen"
@page "/listen/{Tab}"
@using MediaEngine.Web.Components.Library
@using MediaEngine.Web.Models.ViewDTOs

@namespace MediaEngine.Web.Components.Pages

<PageTitle>Listen - Tuvima</PageTitle>

<LibraryPage Tab="@Tab"
             Preset="@Preset" />

@code {
    [Parameter] public string? Tab { get; set; }

    private static readonly LibrarySurfacePreset Preset = new()
    {
        RouteBase = "/listen",
        Title = "Listen",
        VisibleMediaTabs = ["music", "audiobooks"],
        DefaultMediaTab = "music",
        DefaultViewModes = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase)
        {
            ["music"] = "albums",
        },
        HidePrimaryTabs = true,
    };
}
