@page "/collection/{Id:guid}"
@using MediaEngine.Contracts.Details
@using MediaEngine.Web.Components.Details
@using MediaEngine.Web.Services.Integration
@inject IEngineApiClient ApiClient
@inject NavigationManager Nav

@namespace MediaEngine.Web.Components.Pages

<PageTitle>@((_detail?.Title ?? "Collection")) - Tuvima</PageTitle>

@if (_loading)
{
    <div class="tl-detail-page">
        <div class="tl-glass-panel">
            <MudProgressCircular Indeterminate="true" Size="Size.Large" />
        </div>
    </div>
}
else if (_detail is null)
{
    <div class="tl-detail-page">
        <div class="tl-glass-panel">
            <MudText Typo="Typo.h5">Collection unavailable</MudText>
            <MudText Typo="Typo.body2">This collection could not be loaded from your library.</MudText>
        </div>
    </div>
}
else
{
    <DetailPage Model="_detail" OnActionSelected="HandleActionAsync" />
}

@code {
    [Parameter] public Guid Id { get; set; }

    private DetailPageViewModel? _detail;
    private bool _loading = true;

    protected override async Task OnParametersSetAsync()
    {
        _loading = true;
        _detail = await ApiClient.GetDetailPageAsync(DetailEntityType.Collection, Id, DetailPresentationContext.Default);
        _loading = false;
    }

    private Task HandleActionAsync(DetailAction action)
    {
        if (!string.IsNullOrWhiteSpace(action.Route))
            Nav.NavigateTo(action.Route);

        return Task.CompletedTask;
    }
}
