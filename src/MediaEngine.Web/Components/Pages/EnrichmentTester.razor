@page "/tester"
@rendermode InteractiveServer
@namespace MediaEngine.Web.Components.Pages
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/settings/enrichment-tester", replace: true);
    }
}
